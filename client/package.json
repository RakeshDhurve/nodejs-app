{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node setup-env.js development && vite",
    "build": "vite build",
    "build:dev": "node setup-env.js development && vite build --mode development",
    "build:prod": "node setup-env.js production && vite build --mode production",
    "preview": "vite preview",
    "preview:dev": "node setup-env.js development && vite preview --mode development",
    "preview:prod": "node setup-env.js production && vite preview --mode production"
  },
  "dependencies": {
    "pinia": "^3.0.3",
    "vue": "^3.5.17",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^7.0.4"
  }
}
